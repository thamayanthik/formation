package com.formation.algoblitz.linkedlist;

public class Node {
    public int value;
    public Node next;

    public Node(int v, Node nextNode) {
        value = v;
        next = nextNode;
    }
}
